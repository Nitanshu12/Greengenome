# Used by `vite build` only. Points the browser to your Render API when the UI is on Vercel (or any other host).
# Override in Vercel → Settings → Environment Variables if needed.
VITE_API_BASE_URL=https://greengenome.onrender.com
